
import { Navigate } from 'react-router-dom';
import HomePage from './HomePage';

const Index = () => {
  // The Index page just redirects to the HomePage component
  return <HomePage />;
};

export default Index;
